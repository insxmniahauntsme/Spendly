<UserControl x:Class="Spendly.Controls.Pickers.CategoryPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Spendly.Controls"
             x:Name="Root">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/TransactionFilters.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <!-- Top chip -->
        <ToggleButton x:Name="DropButton"
                      Style="{StaticResource TxnFilterChipToggleStyle}"
                      Width="116"
                      IsChecked="{Binding ElementName=Root, Path=IsOpen, Mode=TwoWay}"
                      PreviewMouseRightButtonUp="DropButton_OnPreviewMouseRightButtonUp">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <controls:SvgIcon Width="16"
                                  Height="16"
                                  Margin="0,0,8,0"
                                  Source="{Binding ElementName=Root, Path=IconSource}" />

                <TextBlock Style="{StaticResource TxnFilterChipTextStyle}"
                           Text="{Binding ElementName=Root, Path=Title}" />
            </StackPanel>
        </ToggleButton>

        <!-- Popup -->
        <Popup PlacementTarget="{Binding ElementName=DropButton}"
               Placement="Bottom"
               AllowsTransparency="True"
               StaysOpen="False"
               VerticalOffset="10"
               IsOpen="{Binding ElementName=Root, Path=IsOpen, Mode=TwoWay}"
               DataContext="{Binding ElementName=Root}">

            <Border Style="{StaticResource MonthPickerPopupBorderStyle}"
                    MinWidth="220"
                    Width="220"
                    Padding="2">
                <StackPanel>

                    <!-- Header -->
                    <TextBlock Text="Category"
                               Foreground="{StaticResource Brush.TextPrimary}"
                               FontFamily="Sora"
                               FontSize="13.5"
                               FontWeight="SemiBold"
                               HorizontalAlignment="Center"
                               Margin="0,5,0,10" />

                    <!-- List -->
                    <ListBox Margin="0"
                             Padding="0"
                             ItemsSource="{Binding ItemsSource}"
                             SelectedValuePath="Id"
                             SelectedValue="{Binding SelectedValue, Mode=TwoWay}"
                             Background="Transparent"
                             BorderThickness="0"
                             MaxHeight="260"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                        <ListBox.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource TxnScrollBarStyle}" />
                        </ListBox.Resources>

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Height" Value="34" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="Cursor" Value="Hand" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Grid>
                                                <Border x:Name="Divider"
                                                        Height="1"
                                                        Background="{StaticResource Brush.TextMuted.A10}"
                                                        VerticalAlignment="Bottom"
                                                        Margin="12,0,12,0" />

                                                <Border x:Name="Bd"
                                                        CornerRadius="10"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                        Margin="6,3,6,3">
                                                    <ContentPresenter Margin="10,0,10,0"
                                                                      VerticalAlignment="Center" />
                                                </Border>
                                            </Grid>

                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd"
                                                            Property="Background"
                                                            Value="{StaticResource Brush.TextMuted.A10}" />
                                                    <Setter TargetName="Bd"
                                                            Property="BorderBrush"
                                                            Value="{StaticResource Brush.TextMuted.A10}" />
                                                </Trigger>

                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd"
                                                            Property="Background"
                                                            Value="{StaticResource Brush.Primary.A10}" />
                                                    <Setter TargetName="Bd"
                                                            Property="BorderBrush"
                                                            Value="{StaticResource Brush.Primary.A60}" />
                                                    <Setter TargetName="Divider"
                                                            Property="Opacity"
                                                            Value="0" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="20" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <controls:SvgIcon Grid.Column="0"
                                                      Width="16"
                                                      Height="16"
                                                      VerticalAlignment="Center"
                                                      Source="{Binding IconSource}" />

                                    <TextBlock Grid.Column="1"
                                               Text="{Binding Name}"
                                               Margin="10,0,0,0"
                                               FontFamily="Sora"
                                               FontSize="12.5"
                                               FontWeight="Medium"
                                               Foreground="{StaticResource Brush.TextPrimary}"
                                               VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>